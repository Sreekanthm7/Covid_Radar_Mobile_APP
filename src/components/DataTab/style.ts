import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  dataTabTitle: {
    textAlign: 'center',
    color: '#FFF',
    fontSize: 16,
    fontWeight: '600',
  },
  dataTabValue: {
    textAlign: 'center',
    marginTop: 8,
    color: '#FFF',
    fontSize: 16,
    fontWeight: '500',
  },
});

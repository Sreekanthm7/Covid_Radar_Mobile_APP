import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  covidIcon: {
    width: 180,
    height: 180,
  },
  covidIconContainer: {
    backgroundColor: '#40B14B',
    width: '100%',
    height: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
